# Defense-in-depth for Apache deployments where the project root is public.
# Private runtime state should still live outside the document root when hosting permits it.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule mod_access_compat.c>
    Order allow,deny
    Deny from all
</IfModule>
